# 개발 환경(dev profile) 설정: H2 데이터베이스 사용

# H2 Database Settings
spring.datasource.url=jdbc:h2:mem:testdb # In-memory DB
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 Console Settings (for development debugging)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Database Initialization (for embedded databases like H2)
# spring.sql.init.mode=embedded
# spring.sql.init.mode=always
# spring.sql.init.schema-locations=classpath:schema/h2_schema.sql

# Specify schema location if needed (e.g., if you create schema.sql)
# spring.sql.init.schema-locations=classpath:schema/h2_schema.sql 

